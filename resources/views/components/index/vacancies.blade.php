<!-- Вакансии -->
<section class="section-padding vacancies">
    <video class="background-video" autoplay loop muted><source src="{{ asset('images/temporary/kaliningrad/video/particle.mp4') }}" type="video/mp4"></video>

    <div class="section-wrapper">
        <div class="section-wrapper__head">
            {{ __('Вакансии') }}
            <div class="section-wrapper__desc">
                {{ __('Мы всегда рады пополнению в нашем коллективе!') }}<br>
                {{ __('Работа должна приносить удовольствие!') }}
            </div>
        </div>
        
        <div class="individual-btn">
            <div class="btn btn-type1 pulse" data-open_modal="callback" data-type="2">{{ __('Позвонить') }}</div>
        </div>
    </div>
</section>